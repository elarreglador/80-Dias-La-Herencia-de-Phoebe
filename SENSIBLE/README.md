# SENSIBLE

Directorio excluido por `.gitignore` para secretos.

No versione aquí `.env`, claves de API, certificados ni credenciales.

Estructura sugerida:

```
SENSIBLE/
├── .env                 # variables locales (copia de .env.example)
├── open_meteo_notes.md  # si requiere key en futuro
└── README.md            # este archivo
```
